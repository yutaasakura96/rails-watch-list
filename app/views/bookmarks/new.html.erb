<h1>New Movie for <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, collection: Movie.all, prompt: "Select a movie" %>
  <%= f.input :photo, as: :file %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back to list', @list %>
